# Changelog

All notable changes to `arora-id`. The format follows
[Keep a Changelog](https://keepachangelog.com/); versions follow
[Semantic Versioning](https://semver.org/).

## [1.0.0] - 2026-09-12

### Added

- The 13-emoji base-1024 spelling of a UUID: `encode`, `decode`, `parse`
  (either spelling), `Emoji` (a `Display` adapter), the frozen `ALPHABET`, and
  `v4()` behind the `v4` feature.
