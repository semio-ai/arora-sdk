//! C++ test module — built via build.rs invoking cmake + WASI SDK.
//!
//! The Rust side is intentionally empty. The cargo entry exists so that
//! `cargo build` from the workspace root drives the C++ wasm build.
