# Semio Engine

Semio Engine is a C library (written in Rust) and associated tooling for executing behavior trees in a sandboxed environment.

## Modules

Modules are the building blocks of Semio Engine. Each module exports symbols for other modules to use.